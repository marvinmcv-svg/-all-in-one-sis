{
  "name": "@clearpath/api-gateway",
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "hono": "^4.3.0",
    "@hono/clerk-auth": "^2.0.0",
    "@upstash/ratelimit": "^1.2.0",
    "@upstash/redis": "^1.31.0",
    "@clearpath/shared-types": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^3.60.0",
    "typescript": "^5.4.0"
  }
}
